name = "intern-progress-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Non-secret variables can go here, or be set in the Cloudflare dashboard.
# The NOTION_TOKEN must NOT go here — set it with `wrangler secret put NOTION_TOKEN`
# so it's encrypted and never committed to git.
[vars]
NOTION_DATABASE_ID = "e34513ac1e744e6198c158b66b588153"
ALLOWED_ORIGIN = "https://your-github-username.github.io"
